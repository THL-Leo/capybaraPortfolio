SECRET_KEY=change-me
INVITE_CODE=your-invite-code
DEV_API_URL=http://localhost:5173

PLAID_CLIENT_ID=
PLAID_SECRET=
PLAID_ENV=sandbox

# Production OAuth (optional in sandbox)
PLAID_REDIRECT_URI=
PLAID_WEBHOOK_URL=

# Optional: Fernet key (openssl rand -base64 32). If unset, derived from SECRET_KEY.
ENCRYPTION_KEY=
